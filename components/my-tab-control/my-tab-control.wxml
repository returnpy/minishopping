<!--components/my-tab-control/my-tab-control.wxml-->
<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view class="tab-control-item {{currentIndex === index ? 'act' : ''}}" bindtap="handleTab" data-index="{{index}}"><text>{{item}}</text></view>
  </block>
</view>
