<!--pages/home/child-components/my-recommends.wxml-->
<!-- 推荐 -->
<view class="recommends">
  <block wx:for="{{recommends}}" wx:key="{{index}}">
    <view class="recommends-item">
        <image src="{{item.image}}" bindload="handleImgLoad"></image>
        <view>{{item.title}}</view>
    </view>
  </block>
</view> 
